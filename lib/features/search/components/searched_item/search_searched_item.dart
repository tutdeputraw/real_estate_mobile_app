import 'package:flutter/material.dart';

class SearchSearchedItem extends StatelessWidget {
  const SearchSearchedItem({super.key});

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [
        Text('items:'),
      ],
    );
  }
}
